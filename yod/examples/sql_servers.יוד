// דוגמאות חיבור — MySQL / PostgreSQL
// (דורש שרת רץ; sqlite נשאר ב־examples/sql.יוד)
כלול "SQL"
// MySQL:
// משתנה מסד = SQL.התחבר("mysql", "משתמש:סיסמה@tcp(127.0.0.1:3306)/שם_מסד")
// PostgreSQL:
// משתנה מסד = SQL.התחבר("postgres", "postgres://משתמש:סיסמה@127.0.0.1:5432/שם_מסד?sslmode=disable")
// אותה תחביר פרמטרים (?) בכל המסדים:
// מסד.הפעל("INSERT INTO משתמשים (שם) VALUES (?)", "נועם")
// משתנה שורות = מסד.שלוף("SELECT * FROM משתמשים WHERE שם = ?", "נועם")
// הדפס(מסד.סוג)
// מסד.סגור()
הדפס: "ראה הערות בקובץ — להרצה אמיתית פתחו את ההערות וחברו לשרת"
