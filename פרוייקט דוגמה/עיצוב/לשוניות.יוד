// לשוניות.יוד — Tabs
כלול "פקד.יוד"

מחלקה לשוניות מרחיב פקד {
    פונקציה בנאי(פריטים, נבחר = "", מזהה = "") {
        אם (פריטים == ריק) {
            פריטים = []
        }
        אם (נבחר == ריק) {
            נבחר = ""
        }
        הורה(מזהה)
        זה.פריטים = פריטים
        זה.ערך = נבחר
        זה._שינוי = ריק
        זה._צור("לשוניות", {
            "פריטים": פריטים,
            "ערך": נבחר
        })
    }

    פונקציה קבע_ערך(ט) {
        אם (ט == ריק) {
            ט = ""
        }
        זה.ערך = ט
        זה._שלח_פקודה("קבע_ערך", { "ערך": ט })
    }

    פונקציה קבל_ערך() {
        החזר זה.ערך
    }

    פונקציה בשינוי(פונק) {
        זה._שינוי = פונק
    }
}
