// דוגמת אפליקציה קטנה — מונה עם מחלקה וסגירה
// yod הרץ examples\app_demo.יוד
מחלקה מונה
    פרטי משתנה ערך = 0
    ציבורי פונקציה בנאי התחלה
        זה.ערך = התחלה
    סוף
    ציבורי פונקציה הבא
        זה.ערך = זה.ערך + 1
        החזר זה.ערך
    סוף
    ציבורי פונקציה נוכחי
        החזר זה.ערך
    סוף
סוף

משתנה מ = חדש מונה: 0
הדפס: "התחלה: " + מ.נוכחי()
הדפס: "אחרי הבא: " + מ.הבא()
הדפס: "אחרי הבא: " + מ.הבא()
פונקציה ברכה שם
    החזר "שלום, " + שם + "!"
סוף

הדפס: ברכה("יוד")
