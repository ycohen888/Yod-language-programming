// תיבת_בחירה.יוד — Select / Dropdown
כלול "פקד.יוד"

מחלקה תיבת_בחירה מרחיב פקד {
    פונקציה בנאי(פריטים, נבחר = "", מזהה = "") {
        אם (פריטים == ריק) {
            פריטים = []
        }
        אם (נבחר == ריק) {
            נבחר = ""
        }
        הורה(מזהה)
        זה.פריטים = פריטים
        זה.ערך = נבחר
        זה._שינוי = ריק
        זה._צור("תיבת_בחירה", {
            "פריטים": פריטים,
            "ערך": נבחר
        })
    }

    פונקציה קבע_ערך(ט) {
        אם (ט == ריק) {
            ט = ""
        }
        זה.ערך = ט
        זה._שלח_פקודה("קבע_ערך", { "ערך": ט })
    }

    פונקציה קבל_ערך() {
        החזר זה.ערך
    }

    פונקציה קבע_פריטים(רשימה) {
        אם (רשימה == ריק) {
            רשימה = []
        }
        זה.פריטים = רשימה
        זה._שלח_פקודה("קבע_פריטים", { "פריטים": רשימה })
    }

    פונקציה בשינוי(פונק) {
        זה._שינוי = פונק
    }
}
