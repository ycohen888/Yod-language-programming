# דוגמה: יצירת דוח Excel ושליחה באימייל / Gmail
# Gmail: הגדירו GMAIL_USER ו־GMAIL_APP_PASS (סיסמת אפליקציה)
# SMTP אחר: SMTP_HOST, SMTP_USER, SMTP_PASS, SMTP_FROM, SMTP_TO

כלול "גיליון"
כלול "אימייל"
כלול "מערכת"

משתנה ד = גיליון.חדש()
ד.קבע_תא("A1", "סיכום")
ד.קבע_תא("B1", 42)
ד.שמור_כ("דוח.xlsx")
ד.סגור()

הדפס("נשמר דוח.xlsx")

# שליחה דרך Gmail (הסירו הערה):
# אימייל.גימייל({
#   משתמש: מערכת.סביבה("GMAIL_USER"),
#   סיסמה: מערכת.סביבה("GMAIL_APP_PASS"),
#   אל: מערכת.סביבה("SMTP_TO"),
#   נושא: "דוח יומי",
#   תוכן: "מצורף דוח",
#   קבצים: ["דוח.xlsx"]
# })
